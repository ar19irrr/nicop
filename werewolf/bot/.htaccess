<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # اجازه دسترسی به پوشه‌ها
    RewriteRule ^data/ - [F,L]
    
    # همه درخواست‌ها به index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
